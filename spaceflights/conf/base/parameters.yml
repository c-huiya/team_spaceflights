#predicted target column
target_column: repeat_buyer

#fraction of data used for training
train_fraction: 0.7

#random seed to ensure train/test split is same every time
random_state: 42

#XGBoost hyperparameters
model_params:
  n_estimators: 100 #no. of trees model will build
  random_state: 42 #random seed for model consistency

#place to save trained model
save_model_path: saved_model/saved_model_XGBoost.pkl